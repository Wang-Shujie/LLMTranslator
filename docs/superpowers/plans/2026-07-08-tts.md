# TTS 朗读 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 主界面原文/译文各加一个 🔊 按钮，点击用 edge-tts 在线合成并朗读，播放中可停止，两按钮互斥；预留 `TtsEngine` 离线扩展点。

**Architecture:** `core/tts.py`（纯 async 的 `TtsEngine` 接口 + `EdgeTtsEngine` + 声音-语言映射）+ `ui/tts_player.py`（`TtsPlayer`：worker 线程合成 mp3 → Qt 信号回主线程 → `QMediaPlayer` 播放，代际计数 `_gen` 作废过期合成）。主窗口加 2 个按钮，toggle/switch 逻辑在按钮 handler。

**Tech Stack:** Python 3.12 · PySide6（QtMultimedia `QMediaPlayer`）· edge-tts（在线，微软语音）· 现有 worker 线程 + `asyncio.run` + Qt 信号模式。

**Spec:** `docs/superpowers/specs/2026-07-08-tts-design.md`
**约束：本地实现，不 push。**

---

## 文件结构

| 文件 | 责任 | 动作 |
|---|---|---|
| `src/llm_translator/core/tts.py` | `TtsError`、`TtsEngine` ABC、`LANG_VOICE`、`DEFAULT_VOICE`、`pick_voice`、`EdgeTtsEngine`（纯 async，不依赖 Qt） | 新增 |
| `src/llm_translator/ui/tts_player.py` | `TtsPlayer(QObject)`：合成+播放+停止+代际作废 | 新增 |
| `tests/test_tts.py` | `LANG_VOICE`/`pick_voice`/`EdgeTtsEngine` 纯逻辑单测 | 新增 |
| `src/llm_translator/ui/main_window.py` | 加 2 个 🔊 按钮 + `tts_player` + handler + 图标切换 | 修改 |
| `pyproject.toml` | 依赖加 `edge-tts` | 修改 |
| `build.spec` | `collect_all("edge_tts")` + `PySide6.QtMultimedia` hiddenimport | 修改 |
| `README.md` | 功能区加 TTS + 在线限制说明 | 修改 |

---

## Task 1: 加 edge-tts 依赖

**Files:**
- Modify: `pyproject.toml`

- [ ] **Step 1: 在 `dependencies` 列表加 `edge-tts`**

把 `pyproject.toml` 第 10–18 行的 `dependencies` 改为（在 `platformdirs` 后加 `edge-tts`）：

```toml
dependencies = [
    "PySide6>=6.6",
    "PySide6-Addons>=6.6",
    "qasync>=0.27",
    "httpx>=0.27",
    "curl_cffi>=0.7",
    "cryptography>=42",
    "platformdirs>=4.2",
    "edge-tts>=6.1",
]
```

- [ ] **Step 2: 安装并验证可导入**

Run: `.venv/Scripts/python.exe -m pip install -e ".[dev]"`
Expected: 安装成功（带 `edge-tts` + `aiohttp` 等）。

Run: `.venv/Scripts/python.exe -c "import edge_tts; print('edge-tts ok')"`
Expected: `edge-tts ok`

- [ ] **Step 3: Commit**

```bash
git add pyproject.toml
git commit -m "feat(tts): add edge-tts dependency"
```

---

## Task 2: core/tts.py — 引擎接口 + 声音映射（TDD）

**Files:**
- Create: `src/llm_translator/core/tts.py`
- Test: `tests/test_tts.py`

- [ ] **Step 1: 写失败测试 `tests/test_tts.py`**

```python
from llm_translator.core.tts import (
    DEFAULT_VOICE,
    LANG_VOICE,
    EdgeTtsEngine,
    TtsEngine,
    TtsError,
    pick_voice,
)


def test_lang_voice_has_common_languages():
    assert LANG_VOICE["zh"].startswith("zh-CN")
    assert LANG_VOICE["en"].startswith("en-US")
    assert LANG_VOICE["ja"].startswith("ja-JP")


def test_pick_voice_known():
    assert pick_voice("zh") == LANG_VOICE["zh"]
    assert pick_voice("en") == LANG_VOICE["en"]


def test_pick_voice_unknown_and_auto_falls_back():
    assert pick_voice("xx") == DEFAULT_VOICE
    assert pick_voice("auto") == DEFAULT_VOICE  # src_lang=auto 兜底


def test_edge_engine_is_tts_engine():
    assert isinstance(EdgeTtsEngine(), TtsEngine)


def test_tts_error_is_exception():
    assert issubclass(TtsError, Exception)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/Scripts/python.exe -m pytest tests/test_tts.py -v`
Expected: FAIL — `No module named 'llm_translator.core.tts'`。

- [ ] **Step 3: 实现 `src/llm_translator/core/tts.py`**

```python
"""TTS 引擎接口 + 在线 edge-tts 实现（离线引擎为预留扩展点）。"""
from __future__ import annotations

from abc import ABC, abstractmethod


class TtsError(Exception):
    """TTS 合成失败。"""


class TtsEngine(ABC):
    """语音合成引擎接口。

    v1 仅有在线实现 EdgeTtsEngine。后续离线引擎（如 OfflineSapiEngine，基于
    pyttsx3 / Windows SAPI）实现同一接口即可接入，调用方（TtsPlayer）无需改动。
    """

    @abstractmethod
    async def synthesize(self, text: str, lang: str) -> bytes:
        """合成语音，返回完整 mp3 字节。lang=语言码(zh/en/ja...)。失败抛 TtsError。"""


# 语言码 → edge-tts 音色（默认自然女声）
LANG_VOICE: dict[str, str] = {
    "zh": "zh-CN-XiaoxiaoNeural",
    "en": "en-US-AriaNeural",
    "ja": "ja-JP-NanamiNeural",
    "ko": "ko-KR-SunHiNeural",
    "fr": "fr-FR-DeniseNeural",
    "de": "de-DE-KatjaNeural",
    "es": "es-ES-ElviraNeural",
    "ru": "ru-RU-DariyaNeural",
    "it": "it-IT-ElsaNeural",
    "pt": "pt-BR-FranciscaNeural",
    "th": "th-TH-PremwadeeNeural",
    "vi": "vi-VN-HoaiMyNeural",
    "ar": "ar-SA-ZariyahNeural",
}
DEFAULT_VOICE = "zh-CN-XiaoxiaoNeural"  # lang 未命中或 "auto" 时兜底


def pick_voice(lang: str) -> str:
    """语言码 → edge-tts 音色；未命中/auto → DEFAULT_VOICE。"""
    return LANG_VOICE.get(lang, DEFAULT_VOICE)


class EdgeTtsEngine(TtsEngine):
    """在线 TTS 引擎（edge-tts / 微软语音）。"""

    async def synthesize(self, text: str, lang: str) -> bytes:
        import edge_tts  # 延迟导入：便于打包收集 + 缺失时清晰报错

        voice = pick_voice(lang)
        try:
            comm = edge_tts.Communicate(text, voice)
            buf = bytearray()
            async for chunk in comm.stream():
                if chunk["type"] == "audio":
                    buf += chunk["data"]
            if not buf:
                raise TtsError("edge-tts 返回空音频")
            return bytes(buf)
        except TtsError:
            raise
        except Exception as e:  # 网络/解析等
            raise TtsError(f"edge-tts 合成失败：{e}") from e
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/Scripts/python.exe -m pytest tests/test_tts.py -v`
Expected: PASS（5 项）。

- [ ] **Step 5: Commit**

```bash
git add src/llm_translator/core/tts.py tests/test_tts.py
git commit -m "feat(tts): TtsEngine interface + EdgeTtsEngine + voice mapping"
```

---

## Task 3: ui/tts_player.py — 播放控制器

**Files:**
- Create: `src/llm_translator/ui/tts_player.py`

> Qt + 多媒体类，靠 headless 构造 + 手动验收（与既有 UI 任务一致，不写联网单测）。

- [ ] **Step 1: 实现 `src/llm_translator/ui/tts_player.py`**

```python
"""TTS 播放控制器：worker 线程合成 mp3 → Qt 信号回主线程 → QMediaPlayer 播放。

代际计数 _gen：每次 play/stop 自增；worker 回来的旧结果 gen != _gen 即丢弃，
避免"停止后又突然响"。QMediaPlayer 只能在 Qt 主线程用，故合成在 worker 线程、
播放经 _synthesized 信号回到主线程。
"""
from __future__ import annotations

import asyncio
import os
import tempfile
import threading

from PySide6.QtCore import QObject, QUrl, Signal, Slot
from PySide6.QtMultimedia import QMediaPlayer

from llm_translator.core.tts import TtsEngine, TtsError


class TtsPlayer(QObject):
    """朗读一段文本：合成（worker 线程）→ 临时 mp3 → QMediaPlayer 播放。

    信号：
      state_changed(str): "idle" | "loading" | "playing"
      error(str): 失败文案
    """

    state_changed = Signal(str)
    error = Signal(str)
    _synthesized = Signal(int, bytes)  # (gen, mp3 bytes) —— worker 线程→主线程

    def __init__(self, engine: TtsEngine, parent=None) -> None:
        super().__init__(parent)
        self._engine = engine
        self._player = QMediaPlayer()
        self._player.mediaStatusChanged.connect(self._on_media_status)
        self._gen = 0
        self._tempfile: str | None = None
        self._synthesized.connect(self._play_bytes)

    def play(self, text: str, lang: str) -> None:
        """开始一次新朗读（旧的在途合成会被作废）。空文本无操作。"""
        text = (text or "").strip()
        if not text:
            return
        self._gen += 1
        gen = self._gen
        self.state_changed.emit("loading")
        engine = self._engine

        def worker() -> None:
            try:
                data = asyncio.run(engine.synthesize(text, lang))
                self._synthesized.emit(gen, data)
            except TtsError as e:
                self._emit_error(gen, str(e))
            except Exception as e:
                self._emit_error(gen, f"朗读失败：{e}")

        threading.Thread(target=worker, daemon=True).start()

    def stop(self) -> None:
        """停止播放并作废在途合成。"""
        self._gen += 1
        self._player.stop()
        self._cleanup_temp()
        self.state_changed.emit("idle")

    @Slot(int, bytes)
    def _play_bytes(self, gen: int, data: bytes) -> None:
        if gen != self._gen:
            return  # 过期合成，丢弃
        self._cleanup_temp()
        fd, path = tempfile.mkstemp(suffix=".mp3")
        try:
            with os.fdopen(fd, "wb") as f:
                f.write(data)
        except OSError:
            self._emit_error(gen, "无法写入临时音频文件")
            return
        self._tempfile = path
        self._player.setSource(QUrl.fromLocalFile(path))
        self._player.play()
        self.state_changed.emit("playing")

    def _emit_error(self, gen: int, msg: str) -> None:
        if gen != self._gen:
            return  # 过期，丢弃
        self.error.emit(msg)
        self.state_changed.emit("idle")

    def _on_media_status(self, status) -> None:
        if status == QMediaPlayer.EndOfMedia:
            self._cleanup_temp()
            self.state_changed.emit("idle")

    def _cleanup_temp(self) -> None:
        if self._tempfile:
            try:
                os.unlink(self._tempfile)
            except OSError:
                pass
            self._tempfile = None
```

- [ ] **Step 2: headless 构造验证**

Run:
```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -c "
from PySide6.QtWidgets import QApplication
app = QApplication([])
from llm_translator.ui.tts_player import TtsPlayer
from llm_translator.core.tts import EdgeTtsEngine
p = TtsPlayer(EdgeTtsEngine())
print('tts_player ok')
"
```
Expected: `tts_player ok`（无 traceback）。

- [ ] **Step 3: 运行全测试套件确认无回归**

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: 全部 PASS（之前 + 新 test_tts 5 项）。

- [ ] **Step 4: Commit**

```bash
git add src/llm_translator/ui/tts_player.py
git commit -m "feat(tts): TtsPlayer playback controller"
```

---

## Task 4: 主窗口集成 — 2 个 🔊 按钮

**Files:**
- Modify: `src/llm_translator/ui/main_window.py`

> 共 6 处改动：导入、`__init__` 建 player、`_build_ui` 加 2 按钮、`_wire_signals` 连线、新增 5 个方法。逐处给精确 old→new。

- [ ] **Step 1: 加导入**

把第 35 行后（`from llm_translator.ui.history_dialog import HistoryDialog` 之后）追加两行：

old:
```python
from llm_translator.ui.settings_dialog import SettingsDialog
from llm_translator.ui.history_dialog import HistoryDialog
```
new:
```python
from llm_translator.ui.settings_dialog import SettingsDialog
from llm_translator.ui.history_dialog import HistoryDialog
from llm_translator.core.tts import EdgeTtsEngine
from llm_translator.ui.tts_player import TtsPlayer
```

- [ ] **Step 2: `__init__` 里建 TtsPlayer + `_active_speak_btn`**

old（第 260–262 行附近）:
```python
        self._current_task = None
        self._tray: QSystemTrayIcon | None = None
        self._build_translator()
```
new:
```python
        self._current_task = None
        self._tray: QSystemTrayIcon | None = None
        self._active_speak_btn: QPushButton | None = None
        self._build_translator()
        self.tts_player = TtsPlayer(EdgeTtsEngine(), self)
```

- [ ] **Step 3: 源文本区加"🔊 原文"按钮**

old（`_build_ui` 源文本区的 `col`，第 381–383 行）:
```python
        col = QVBoxLayout()
        col.addWidget(self.clear_btn)
        col.addStretch()
```
new:
```python
        col = QVBoxLayout()
        col.addWidget(self.clear_btn)
        self.src_speak_btn = QPushButton("🔊 原文")
        col.addWidget(self.src_speak_btn)
        col.addStretch()
```

- [ ] **Step 4: 译文区加"🔊 译文"按钮**

old（译文区的 `out_col`，第 398–401 行）:
```python
        out_col = QVBoxLayout()
        self.copy_btn = QPushButton("📋 复制")
        out_col.addWidget(self.copy_btn)
        out_col.addStretch()
```
new:
```python
        out_col = QVBoxLayout()
        self.copy_btn = QPushButton("📋 复制")
        out_col.addWidget(self.copy_btn)
        self.tgt_speak_btn = QPushButton("🔊 译文")
        out_col.addWidget(self.tgt_speak_btn)
        out_col.addStretch()
```

- [ ] **Step 5: `_wire_signals` 连线**

old（第 420–422 行附近）:
```python
        self.translate_btn.clicked.connect(self.on_translate)
        self.clear_btn.clicked.connect(lambda: self.src_edit.clear())
        self.copy_btn.clicked.connect(self.on_copy)
```
new:
```python
        self.translate_btn.clicked.connect(self.on_translate)
        self.clear_btn.clicked.connect(lambda: self.src_edit.clear())
        self.copy_btn.clicked.connect(self.on_copy)
        self.src_speak_btn.clicked.connect(self.on_speak_source)
        self.tgt_speak_btn.clicked.connect(self.on_speak_target)
        self.tts_player.state_changed.connect(self._on_tts_state)
        self.tts_player.error.connect(self._on_tts_error)
```

- [ ] **Step 6: 新增 5 个方法**

在 `on_copy` 方法**之后**（第 486 行 `QApplication.clipboard()...` 那行所在方法之后）插入：

```python
    def on_speak_source(self) -> None:
        """朗读原文：点当前在播按钮=停止；否则切换到原文。"""
        if self._active_speak_btn is self.src_speak_btn:
            self.tts_player.stop()
            return
        text = self.src_edit.toPlainText().strip()
        if not text:
            self.status.showMessage("没有可朗读的原文", 2000)
            return
        self._active_speak_btn = self.src_speak_btn
        self.tts_player.play(text, self.src_combo.currentData())

    def on_speak_target(self) -> None:
        """朗读译文：点当前在播按钮=停止；否则切换到译文。"""
        if self._active_speak_btn is self.tgt_speak_btn:
            self.tts_player.stop()
            return
        text = self.tgt_edit.toPlainText().strip()
        if not text:
            self.status.showMessage("没有可朗读的译文", 2000)
            return
        self._active_speak_btn = self.tgt_speak_btn
        self.tts_player.play(text, self.tgt_combo.currentData())

    def _on_tts_state(self, state: str) -> None:
        """按播放状态切换活动按钮的图标/文字。"""
        btn = self._active_speak_btn
        if btn is None:
            return
        if state == "playing":
            btn.setText("⏹ 停止")
        elif state == "loading":
            btn.setText("⏳ …")
        elif state == "idle":
            self._reset_speak_btn(btn)
            self._active_speak_btn = None

    def _on_tts_error(self, msg: str) -> None:
        self.status.showMessage(f"朗读失败：{msg}", 5000)

    def _reset_speak_btn(self, btn: QPushButton) -> None:
        if btn is self.src_speak_btn:
            btn.setText("🔊 原文")
        elif btn is self.tgt_speak_btn:
            btn.setText("🔊 译文")
```

- [ ] **Step 7: headless 整窗构造验证（含新按钮）**

Run:
```bash
QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -c "
import asyncio
from PySide6.QtWidgets import QApplication
app = QApplication([])
from llm_translator.ui.main_window import MainWindow
w = MainWindow(); w.show()
assert w.src_speak_btn.text() == '🔊 原文'
assert w.tgt_speak_btn.text() == '🔊 译文'
assert w.tts_player is not None
print('MainWindow + TTS buttons OK')
"
```
Expected: `MainWindow + TTS buttons OK`（无 traceback）。

- [ ] **Step 8: 全测试套件无回归**

Run: `.venv/Scripts/python.exe -m pytest -q`
Expected: 全部 PASS。

- [ ] **Step 9: Commit**

```bash
git add src/llm_translator/ui/main_window.py
git commit -m "feat(tts): wire speak buttons into main window"
```

---

## Task 5: build.spec — 打包 edge-tts + QtMultimedia

**Files:**
- Modify: `build.spec`

- [ ] **Step 1: 加 edge-tts 的 collect_all**

old（第 9–11 行）:
```python
# wasmtime（DeepSeek 网页 PoW 用）的平台 native dll（win32-x86_64/_wasmtime.dll）由 ctypes
# 运行时加载，PyInstaller 静态分析看不见，必须整体收集包的 datas/binaries/submodules
_wasmtime_datas, _wasmtime_binaries, _wasmtime_hi = collect_all("wasmtime")
```
new:
```python
# wasmtime（DeepSeek 网页 PoW 用）的平台 native dll（win32-x86_64/_wasmtime.dll）由 ctypes
# 运行时加载，PyInstaller 静态分析看不见，必须整体收集包的 datas/binaries/submodules
_wasmtime_datas, _wasmtime_binaries, _wasmtime_hi = collect_all("wasmtime")

# edge-tts（TTS 朗读）在 EdgeTtsEngine 内延迟 import，静态分析看不见，整体收集
_edge_datas, _edge_binaries, _edge_hi = collect_all("edge_tts")
```

- [ ] **Step 2: binaries 行加 edge 二进制**

old（第 29 行）:
```python
    binaries=[*curl_cffi_binaries, *_wasmtime_binaries],
```
new:
```python
    binaries=[*curl_cffi_binaries, *_wasmtime_binaries, *_edge_binaries],
```

- [ ] **Step 3: datas 加 edge 数据**

old（第 30–35 行）:
```python
    datas=[
        ("assets/light.qss", "assets"),
        # DeepSeek 网页 PoW 的 WASM（随包；wasmtime/numpy 为可选依赖，需另装 [web]）
        ("src/llm_translator/providers/web/wasm", "llm_translator/providers/web/wasm"),
        *_wasmtime_datas,
    ],
```
new:
```python
    datas=[
        ("assets/light.qss", "assets"),
        # DeepSeek 网页 PoW 的 WASM（随包；wasmtime/numpy 为可选依赖，需另装 [web]）
        ("src/llm_translator/providers/web/wasm", "llm_translator/providers/web/wasm"),
        *_wasmtime_datas,
        *_edge_datas,
    ],
```

- [ ] **Step 4: hiddenimports 加 QtMultimedia + edge 子模块**

old（第 36–50 行）:
```python
    hiddenimports=[
        "curl_cffi",
        "curl_cffi.requests",
        "curl_cffi._wrapper",
        "PySide6.QtWebEngineCore",
        "PySide6.QtWebEngineWidgets",
        "PySide6.QtWebEngineQuick",
        "qasync",
        "cryptography",
        "numpy",
        # 注册表用 importlib 懒加载 web providers，PyInstaller 静态分析看不见，
        # 必须显式收集整个 llm_translator 包（含 glm/kimi/deepseek/login_dialog 等）
        *collect_submodules("llm_translator"),
        *_wasmtime_hi,
    ],
```
new:
```python
    hiddenimports=[
        "curl_cffi",
        "curl_cffi.requests",
        "curl_cffi._wrapper",
        "PySide6.QtWebEngineCore",
        "PySide6.QtWebEngineWidgets",
        "PySide6.QtWebEngineQuick",
        "PySide6.QtMultimedia",
        "qasync",
        "cryptography",
        "numpy",
        # 注册表用 importlib 懒加载 web providers，PyInstaller 静态分析看不见，
        # 必须显式收集整个 llm_translator 包（含 glm/kimi/deepseek/login_dialog/tts 等）
        *collect_submodules("llm_translator"),
        *_wasmtime_hi,
        *_edge_hi,
    ],
```

- [ ] **Step 5: 语法校验 spec**

Run: `.venv/Scripts/python.exe -c "import ast; ast.parse(open('build.spec',encoding='utf-8').read()); print('build.spec syntax ok')"`
Expected: `build.spec syntax ok`。

> 完整 PyInstaller 重打包很慢（数分钟）且体积变大，**留到下一版发布时再做**；开发期从 venv 跑即含 TTS。本任务只改 spec + 语法校验。

- [ ] **Step 6: Commit**

```bash
git add build.spec
git commit -m "build(tts): bundle edge-tts + QtMultimedia in PyInstaller spec"
```

---

## Task 6: README — TTS 功能 + 在线限制说明

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 功能区加 TTS 条目**

old（第 5–9 行）:
```markdown
## 功能（v0.1 MVP）
- 文本翻译，流式输出（打字机效果）
- 翻译历史记录（搜索/清空）
- 多模型配置：付费 API（填 Key）+ 网页免费（内嵌登录）
- Windows 一键安装
```
new:
```markdown
## 功能
- 文本翻译，流式输出（打字机效果）
- 翻译历史记录（搜索/清空）
- 多模型配置：付费 API（填 Key）+ 网页免费（内嵌登录）
- TTS 朗读：原文/译文各一个 🔊 按钮，点击朗读（在线）
- Windows 一键安装
```

- [ ] **Step 2: 在"声明"段前加 TTS 在线说明**

old（第 30–32 行）:
```markdown
4. 回主界面选模型，输入文本，Ctrl+Enter 翻译。

## 声明
```
new:
```markdown
4. 回主界面选模型，输入文本，Ctrl+Enter 翻译。

## TTS 朗读（在线）
TTS 朗读当前仅支持在线（使用 `edge-tts` / 微软语音），需要联网。代码已预留 `TtsEngine` 接口，后续版本将加入离线语音引擎（如 Windows 系统 SAPI）。

## 声明
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs(tts): document TTS feature + online-only limitation"
```

---

## 完成后：整体验收

- [ ] **全测试套件**：`.venv/Scripts/python.exe -m pytest -q` → 全绿。
- [ ] **headless 整窗 + 按钮**：Task 4 Step 7 的 smoke test 通过。
- [ ] **手动验收清单**（spec §9，需真实联网 + 真实 provider）：
```
[ ] 点原文 🔊 → 听到原文朗读（按 src 语言音色）
[ ] 点译文 🔊 → 听到译文朗读（按 tgt 语言音色）
[ ] 播放中按钮变 ⏹ 停止，再点停止；播完自动还原 🔊
[ ] 朗读原文时点译文 🔊 → 先停原文再播译文（互斥）
[ ] 拔网后点 🔊 → 状态栏提示"朗读失败"，不崩溃
[ ] 清空文本后点 🔊 → 状态栏"没有可朗读的原文/译文"，不报错
```

> 手动验收项需你本人在真实显示器 + 联网环境下跑（自动测试覆盖不到 edge-tts 联网与音频播放）。
